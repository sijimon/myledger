# Normalize line endings. Force LF for files that run on Linux so a Windows
# checkout never introduces CRLF (which would break shell shebangs, etc.).
* text=auto
*.sh      text eol=lf
Dockerfile text eol=lf
*.yml     text eol=lf
*.yaml    text eol=lf
